don't show node selector on agent page, show only graph and toplevelinvoke index.
then on invoke page, show agent graph _and_ invoke graph (paginated)

I have access to Historical* types

graph ui libraries
  dagre auto layout

decide what to show next to graph
  1. toplevel invokes
   	 not filterable by clicking on node
  2. list of node invokes (filterable by node)

version dropdown (default to latest version)
agents-historical-info {version->graph}

