color code based on node/agg/start/etc (marker for result)
emits panel with arguments/nodeids, that click for nav
currently selected node coloring

depth mapping by node count, not depth count (query topology does that automatically)

mark node if it has result (AgentResult), don't render nil

agg-invoke-id groups?


fix spacing on paginate buttons
fix agg node not showing up


pagination
waterfall view
node details

directed edges
display inputs in the node
in node details, click on emits so navigate to that node
more visible which node is selected

pagination (try example with 20k nodes)
try minimap

node details look nice (like langsmith)
show agent graph and invocation side by side

user/username/login flow
keep going through langgaraph


version dropdown (default to latest version)
