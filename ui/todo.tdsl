trace / graph / fork {
  (with a little text explaining under)
}

stats page has xyflowgraph rendering for agent {
  has version dropdown (includes "overall" option)
}

two graph views (one has invocation specific stats, one has global stats)

collapsible side panel (left)

stats {
  tokens sohuld be next to model calls (under it)
  cut api calls, "calls"
  make stats panel more concise / shorter
}

agentgraph replaces invocation graph

overall _invocation_ stats { (side panel collapsible)
  execution time
  tokens
  writes/reads to stores
  model calls
}

edn=>json (and label that says its json)

remove pagination?

tests for webserver backend


agent page (boxes with expand links)

link to run page (not interactive), agent graph (pretty small, zoom) , stats summary, recent invokes (all invockes button), alerts box

retries with the llm api provider (retry-after header) integrated with node retries
  include retry stats in the stats

tracing currently in flight (polling, indicator, pagination complicates this)

get rid of intermediate agent info screen
react-query/mutationify the mutations for agent creation
use a library for form state (the one recommended by uix readme)
"add current trace to dataset" button 
depth mapping by node count, not depth count (query topology does that automatically)

agg-invoke-id groups?

waterfall view

user/username/login flow

version dropdown (default to latest version)

display retries in the node
