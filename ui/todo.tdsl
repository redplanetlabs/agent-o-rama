test out rednering a loop in agent graph

arrow down instead of see all invocations

get rid of the boxes, replace with one big textearea.

special case of agg fork ui. first box is agg result, agg start result (add label)

have one of the traces be 5 args (scroll horizontally) (make them resizable)

timeseries graphs on agent stats page {
  like rama cluster ui, scroll of many timeseries (granularity like rama cluster ui)
}
edn=>json (and label that says its json)

remove pagination?

tests for webserver backend

retries with the llm api provider (retry-after header) integrated with node retries
  include retry stats in the stats

tracing currently in flight (polling, indicator, pagination complicates this)

use a library for form state (the one recommended by uix readme)
"add current trace to dataset" button 
depth mapping by node count, not depth count (query topology does that automatically)

user/username/login flow
display retries in the node

langgraph {
  what happens when a single node is simultaneously executing at the same time (a loop)
}
